  var body = {
    fingerprint: process.env.FINGERPRINT,
    fiat: this.credit.fiat,
    satoshis: this.credit.satoshis,
    currency: this.currency,
    data_session: this.trader.sessionId
  }
  var post_options = {
    headers: {
      'Accept': 'application/vnd.bitstop.v1',
      'Bitstop-Token': process.env.BITSTOP_TOKEN
    }
  }
  needle.post("https://api.takekontrol.co/companies/send_delete_reference", body, post_options);